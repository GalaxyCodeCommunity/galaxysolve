# Uzavrene zaborky

Vasim ukolem bude rict kolik je v retezci uzavrenych zavorek.

Priklad:

```

$string = 'cau (mega (dobre)) (fakt jo)'

brackets($string) // 3

$string = 'cau (mega (dobre)) (fakt jo'

brackets($string) // 2

$string = 'cau (mega (dobre)) fakt jo)'

brackets($string) // 2
